{
  "name": "lambda-cdk-playground",
  "version": "0.1.0",
  "bin": {
    "lambda-cdk-playground": "bin/lambda-cdk-playground.js"
  },
  "scripts": {
    "build": "tsc && pnpm run build:lambda",
    "build:lambda": "tsc -p lambda/tsconfig.json",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "pnpm run build && cdk deploy",
    "synth": "pnpm run build && cdk synth"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "@types/aws-lambda": "^8.10.145",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "aws-cdk": "2.1024.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.208.0",
    "constructs": "^10.0.0"
  }
}
